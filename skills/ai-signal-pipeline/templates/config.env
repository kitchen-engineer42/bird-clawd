# ai-signal-pipeline — per-install config (sourced by bin/*.sh)
# Set your X/Twitter handle WITHOUT the leading @ (the account bird is logged into).
# Find it with: bird whoami
X_HANDLE="your_x_handle"
