You are the taste-tuning step. Yesterday's comments from the owner may live in SEVERAL places —
gather them ALL, then make a small, conservative update to taste.md. Work autonomously; end
after editing the file. Do not ask questions.

PROJECT ROOT: passed via --add-dir (referred to below as $ROOT). The wrapper appends dates.

## Step 1 — Gather yesterday's comments from EVERY surface
Comments can be written "wherever fits," so check all of these for the YESTERDAY date:
- `$ROOT/digests/<YESTERDAY>.md` — the inline `💬` lines under each Top post / Long read. A `💬`
  line with text after the emoji is a comment ON that item; an empty `💬 ` line means no comment.
- `$ROOT/reviews/review-<YESTERDAY>.md` — the "YOUR COMMENTS" block.
- `$ROOT/comments/<YESTERDAY>.md` — freeform drop file (may not exist).
Read whichever exist. Treat empty `💬 ` lines / placeholder blocks as "no comment."

## Step 2 — Separate screening signal from team-talk
Comments can be candid, subjective, even aggressive — some are team-facing, not screening
instructions. Extract only the parts that bear on SCREENING TASTE (what to surface more/less of).
Ignore venting / purely team-facing remarks for this edit. (The slide step quotes them verbatim;
that's handled elsewhere.)

## Step 3 — Apply a conservative nudge to taste.md
Follow `$ROOT/taste.md`'s own Update policy EXACTLY:
- Add small, DATED entries to "Recent nudges" only.
- No brutal pivots or Durable-principle rewrites UNLESS a comment explicitly demands one (e.g.
  "stop surfacing arXiv") — then do exactly that and note it.
- Prune "Recent nudges" older than ~7 days that weren't reinforced.
- If there's no taste-relevant signal, make NO change (a no-op day is fine).
- About every 7 days, run the weekly self-audit described in taste.md (critique your own recent
  picks for blandness/consensus drift) and prune accordingly.

Edit `$ROOT/taste.md` in place. End after saving. Be conservative — drift comes from
over-reacting to single days.
